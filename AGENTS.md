# AGENTS.md

This file contains essential information to help agents working with this repository avoid mistakes and ramp up quickly. Everything here should be specific to this codebase and answer: "Would an agent likely miss this without help?"

## Quick Start

- **Repository entrypoint**: This is a monorepo with multiple packages managed by [package manager]
- **Main application**: [application name or main package]
- **Development setup**: Run `npm install` or `yarn install` from root
- **Run tests**: `npm test` or `yarn test`

## Key Commands

- Start development server: `npm run dev` or `yarn dev`
- Lint code: `npm run lint` or `yarn lint`
- Run typecheck: `npm run typecheck` or `yarn typecheck`
- Build for production: `npm run build` or `yarn build`

## Architecture Notes

- [Important architectural decisions or constraints]
- Package boundaries and relationships
- Entry points for different components

## Testing

- How to run individual tests
- Test setup requirements
- Any special testing configurations

## Environment & Setup

- Required environment variables
- Special setup steps beyond typical install
- Dev server configuration

## Toolchain Quirks

- Generated code or code generation workflows
- Special build artifacts or deployment processes
- Framework-specific considerations or non-obvious behavior
